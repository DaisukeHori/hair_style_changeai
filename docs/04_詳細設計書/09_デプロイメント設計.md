# 第9章 デプロイメント設計

## 9.1 環境構成

| 環境 | URL | 用途 |
|------|-----|------|
| Development | localhost:5173 | 開発 |
| Staging | staging.example.com | テスト |
| Production | app.example.com | 本番 |

## 9.2 CI/CDパイプライン

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test
      - run: npm run build

  deploy-staging:
    needs: test
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}

  deploy-production:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
```

## 9.3 環境変数管理

```bash
# .env.example
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_NANOBANANA_API_KEY=
VITE_SENTRY_DSN=
VITE_APP_ENV=development
```

Vercel/Supabaseダッシュボードで環境別に設定。

## 9.4 デプロイ手順

### 本番デプロイ
1. develop → main にPR作成
2. レビュー・承認
3. マージ → 自動デプロイ
4. 動作確認

### ロールバック
1. Vercelダッシュボード → Deployments
2. 前回正常バージョンを選択
3. "Promote to Production"
